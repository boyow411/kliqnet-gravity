# Release status

The redesign is staged on `codex/agency-portfolio-redesign`. Local TypeScript, content tests, security regression, production build and 37 public-route checks passed. Desktop/mobile browser checks include an actual saved enquiry and keyboard-accessible image enlargement.

A Vercel preview is being prepared from this source. The public production domain has not been switched to this design. The additive database schema and curated editorial records are ready; prior editorial records were backed up before applying changes.

The owner’s next action is to review the preview and approve the production website switch. Existing saved preferences require that final release approval. Google Business Profile and social publishing remain the next iteration.
