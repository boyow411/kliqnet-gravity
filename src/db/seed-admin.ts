// Editorial records now use the validated release scripts, with backups and explicit apply.
console.log(
  "Run npx tsx scripts/publish-portfolio.ts and npx tsx scripts/publish-insights.ts to validate. Add --apply only when intentionally publishing their seed content.",
);
export {};
